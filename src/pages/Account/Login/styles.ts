import { StyleSheet } from 'react-native';
import { px2rem } from '@/utils/px2rem';

export default StyleSheet.create({
  warp: {
    backgroundColor: '#fff',
    flex: 1,
  },
  button: {
    marginTop: px2rem(20),
  },
});
